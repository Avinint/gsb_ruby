1) installer ruby 2.2.6 (probleme du plus récent avec la gem bcrypt)

2) gem update --system pour mettre a jour
gem install bundler

3) recupérer le répertoire sur git
aller dans le répertoire et lancer bundle install :
bundler va installer toutes les gems dans gemfile et créer un fichier gemlock

4) ne pas oublier de lancer xampp si on teste en local :p